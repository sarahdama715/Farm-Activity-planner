'use client';
import Link from 'next/link';
import { useParams } from 'next/navigation';
import { useApp } from '@/components/AppProvider';
export default function PlanSummary() { const { id } = useParams<{ id: string }>(); const { plans } = useApp(); const plan = plans.find((item) => item.id === id); if (!plan) return <div><h1 className="text-3xl font-bold text-gray-900">Plan not found</h1><Link href="/" className="mt-4 inline-block text-green-700 underline">Back to dashboard</Link></div>; return <div className="max-w-3xl space-y-6"><div><Link href="/" className="text-green-700 underline">Back to dashboard</Link><h1 className="mt-3 text-4xl font-bold text-gray-900">{plan.crop} plan summary</h1><p className="mt-2 text-gray-700">{plan.city || plan.location}, {plan.country || ''}</p></div><section className="rounded-lg bg-white p-6 shadow"><dl className="grid gap-4 md:grid-cols-2 text-gray-800"><div><dt className="font-semibold">Status</dt><dd>{plan.status || 'Planted'}</dd></div><div><dt className="font-semibold">Planting date</dt><dd>{plan.plantingDate}</dd></div><div><dt className="font-semibold">Farm size</dt><dd>{plan.acres} acres</dd></div><div><dt className="font-semibold">Workers</dt><dd>{plan.workers}</dd></div></dl><h2 className="mt-6 text-xl font-bold text-gray-900">Resources recorded</h2><p className="mt-2 text-gray-800">{plan.resources}</p><h2 className="mt-6 text-xl font-bold text-gray-900">Weather location</h2><p className="mt-2 text-gray-800">{plan.weather}</p></section></div>; }
